fastapi==0.111.0
uvicorn[standard]==0.30.1
motor==3.5.0
pymongo==4.8.0
pydantic==2.8.2
pydantic-settings==2.3.4
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend probes `bcrypt.__about__`, which was removed
# in bcrypt>=4.1, causing "password cannot be longer than 72 bytes" errors on
# every hash/verify call. Pin bcrypt to a version passlib can introspect.
bcrypt==4.0.1
python-multipart==0.0.9
python-dotenv==1.0.1
